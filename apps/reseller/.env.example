# Bayi paneli — varsayılan port 4001 (SaaS/POS’tan ayrı origin)
DEV_SERVER_PORT=4001
DEV_SERVER_HOST=127.0.0.1
# API (Vite proxy hedefi)
# apps/api portu ile aynı olmalı (bu repo ayarında 5000)
API_PROXY_TARGET=http://127.0.0.1:5000
