# Bayi paneli — varsayılan port 4001 (SaaS/POS’tan ayrı origin)
DEV_SERVER_PORT=4001
DEV_SERVER_HOST=127.0.0.1
# API (Vite proxy hedefi) — apps/api varsayılanı 3101; API .env'de PORT=5000 ise 5000 yazın
API_PROXY_TARGET=http://127.0.0.1:3101
